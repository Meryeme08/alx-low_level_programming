#include "main.h"

/**
 * _strlen - returns the length of a string
 * @s: string
 * Return: length
 */
int _strlen(char *s)
{
	int longi;

	for (longi = 0; s[longi] != '\0'; longi++)
	{
		continue;
	}

	return (longi);
}
